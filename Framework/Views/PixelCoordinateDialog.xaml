
<Window x:Class="Framework.Views.PixelCoordinateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入像素坐标"
        Height="200"
        Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="请输入当前点的像素坐标:"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <TextBlock Text="像素X:"
                       Width="60"
                       VerticalAlignment="Center" />
            <TextBox Text="{Binding PixelX}"
                     Width="100"
                     VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">
            <TextBlock Text="像素Y:"
                       Width="60"
                       VerticalAlignment="Center" />
            <TextBox Text="{Binding PixelY}"
                     Width="100"
                     VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="确定"
                    Click="OK_Click"
                    Width="80"
                    Margin="0,0,10,0" />
            <Button Content="取消"
                    Click="Cancel_Click"
                    Width="80" />
        </StackPanel>
    </Grid>
</Window>