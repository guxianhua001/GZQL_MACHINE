<!-- ErrorDialog.xaml -->
<UserControl x:Class="ModuleCore.Views.ErrorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:Framework.Converters;assembly=Framework"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <conv:CriticalToBrushConverter x:Key="CriticalToBrushConverter" />
            <conv:CriticalToIconConverter x:Key="CriticalToIconConverter" />
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="Deeppurple"
                                             SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <materialDesign:Card Margin="10"
                         Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="0,5">
                <materialDesign:PackIcon Kind="{Binding IsCritical, Converter={StaticResource CriticalToIconConverter}}"
                                         Width="28"
                                         Height="28"
                                         Margin="0,0,10,0"
                                         Foreground="{Binding IsCritical, Converter={StaticResource CriticalToBrushConverter}}" />

                <TextBlock Text="{Binding Title}"
                           FontSize="18"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />
            </StackPanel>

            <!-- 错误消息 -->
            <TextBlock Grid.Row="1"
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       Margin="10"
                       Foreground="{DynamicResource MaterialDesignBody}" />

            <!-- 操作按钮 -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,15,0,5">
                <Button Content="{lang:Lang Confirm}"
                        Command="{Binding CloseDialogCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        IsCancel="True"
                        Foreground="{Binding IsCritical, Converter={StaticResource CriticalToBrushConverter}}" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>
