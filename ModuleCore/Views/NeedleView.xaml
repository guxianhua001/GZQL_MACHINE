<UserControl x:Class="ModuleCore.Views.NeedleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleCore"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:ModuleCore.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:ProgressColorConverter x:Key="ProgressColorConverter" />
    </UserControl.Resources>
    
    <Grid Margin="16">
        <ItemsControl ItemsSource="{Binding Needles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"
                                 Rows="2" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Margin="8"
                                         Padding="16">
                        <StackPanel>
                            <TextBlock Text="{Binding Id, StringFormat='{}针头 {0}号'}"
                                       Style="{StaticResource MaterialDesignHeadline6TextBlock}" />

                            <ProgressBar Value="{Binding CurrentCount}"
                                         Maximum="{Binding MaxCount}"
                                         Height="20"
                                         Margin="0 8"
                                         Foreground="{Binding Converter={StaticResource ProgressColorConverter}}" />
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <TextBox Text="{Binding MaxCount, UpdateSourceTrigger=PropertyChanged}"
                                         materialDesign:HintAssist.Hint="最大值"
                                         Width="100"
                                         Margin="0 0 8 0" />
                                <Button Command="{Binding DataContext.ResetMaxCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Content="重置最大值"
                                        Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </StackPanel>
                            <TextBlock Text="{Binding CurrentCount, StringFormat='{}当前使用次数: {0}'}"
                                       Margin="0 8"
                                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}" />
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <Button Command="{Binding DataContext.IncrementCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Content="+1"
                                        IsEnabled="{Binding IsEnabled}"
                                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}" />
                                <Button Command="{Binding DataContext.ClearCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Content="清除"
                                        Margin="8 0 0 0"
                                        Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>


</UserControl>
