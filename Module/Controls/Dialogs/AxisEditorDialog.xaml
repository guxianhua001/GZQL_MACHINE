<UserControl x:Class="Module.Views.AxisEditorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             Width="400"
             Height="Auto">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding Group}"
                     materialDesign:HintAssist.Hint="{lang:Lang Dialog_Group}"
                     Margin="0,4" />
            <TextBox Text="{Binding Name}"
                     materialDesign:HintAssist.Hint="{lang:Lang Dialog_Name}"
                     Margin="0,4" />
            <TextBox Text="{Binding Description}"
                     materialDesign:HintAssist.Hint="{lang:Lang Dialog_Description}"
                     Margin="0,4" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="{lang:Lang Dialog_Cancel}"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0" />
            <Button Content="{lang:Lang Dialog_Save}"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </Grid>
</UserControl>
