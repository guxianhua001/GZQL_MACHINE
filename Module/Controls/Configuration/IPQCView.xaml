<UserControl x:Class="Module.Views.IPQCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:converters="clr-namespace:Module.Converters"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             prism:ViewModelLocator.AutoWireViewModel="True"
             MaxHeight="480">
    <UserControl.Resources>
        <!-- 转换器 -->
        <converters:StatusToBrushConverter x:Key="StatusToBrushConverter" />
    </UserControl.Resources>
    <Border Padding="16"
            Background="{DynamicResource MaterialDesignCardBackground}"
            CornerRadius="4">
        <!-- 整体滚动条 -->
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>

                <!-- 标题 -->
                <TextBlock Text="IPQC Configuration &amp; Execution"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,16"/>

                <!-- 第一部分：IPQC 参数配置 -->
                <GroupBox Header="Examples of IPQC Parameters" Margin="0,0,0,16">
                    <Grid Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Site" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding Sites}"
                                  SelectedItem="{Binding SelectedSite}"
                                  Width="150"
                                  Margin="0,0,8,0"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Check Type" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="3"
                                  ItemsSource="{Binding CheckTypes}"
                                  SelectedItem="{Binding SelectedCheckType}"
                                  Width="180"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{lang:Lang Camera}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="1" Grid.Column="1"
                                  ItemsSource="{Binding Cameras}"
                                  SelectedItem="{Binding SelectedCamera}"
                                  Width="150"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Tolerance (μm)" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ToleranceXY}" Width="100"/>

                    </Grid>
                </GroupBox>

                <!-- 第二部分：运动流程编辑表格 -->
                <GroupBox Header="Inspection Motion Sequence" Margin="0,0,0,16">
                    <StackPanel>
                        <DataGrid ItemsSource="{Binding InspectionMoves}"
                                  SelectedItem="{Binding SelectedInspectionMove}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  materialDesign:DataGridAssist.CellPadding="4"
                                  MaxHeight="250">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding SubSeq}" Width="40" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Axis" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Axis}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.AvailableAxes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedItem="{Binding Axis, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Target (mm/deg)" Binding="{Binding Offset}" Width="100"/>
                                <DataGridTextColumn Header="Speed (%)" Binding="{Binding Speed}" Width="70"/>
                                <DataGridTextColumn Header="{lang:Lang WaypointEdit_Column_Actions}" Binding="{Binding ActionType}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang Description}" Binding="{Binding Description}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                            <Button Content="➕ Add Move" Margin="4" Command="{Binding AddMoveCommand}"/>
                            <Button Content="🗑 Delete" Margin="4" Command="{Binding DeleteMoveCommand}"/>
                            <Button Content="↑" Margin="4" Command="{Binding MoveUpCommand}"/>
                            <Button Content="↓" Margin="4" Command="{Binding MoveDownCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- 第三部分：检测结果展示 -->
                <GroupBox Header="{lang:Lang InspectionDetails}" Margin="0,0,0,16">
                    <StackPanel>
                        <!-- 统计卡片 -->
                        <Border Background="#F0F4F8" CornerRadius="6" Padding="12" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="Total Assemblies" FontWeight="Bold" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding TotalAssemblies}" FontSize="20" FontWeight="Bold" Foreground="#2c3e50"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="{lang:Lang AssemblyStep_PassCountLabel}" FontWeight="Bold" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding PassCount}" FontSize="20" FontWeight="Bold" Foreground="Green"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="{lang:Lang AssemblyStep_FailCountLabel}" FontWeight="Bold" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding FailCount}" FontSize="20" FontWeight="Bold" Foreground="Red"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                                    <TextBlock Text="{lang:Lang AssemblyStep_LastMeasureTimeLabel}" FontWeight="Bold" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding LastMeasureTime}" FontSize="12" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 详细测量数据表格 -->
                        <DataGrid ItemsSource="{Binding MeasurementData}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  materialDesign:DataGridAssist.CellPadding="4"
                                  MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="130"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_AssemblyId}" Binding="{Binding AssemblyId}" Width="120"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Status}" Binding="{Binding Status}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Operator}" Binding="{Binding Operator}" Width="80"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Recipe}" Binding="{Binding Recipe}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_OverallCycleTime}" Binding="{Binding TotalCycleTime}" Width="80"/>
                                <DataGridTextColumn Header="Actuator" Binding="{Binding ActuatorSeq}" Width="70"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_ActuatorId}" Binding="{Binding ActuatorId}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Pillar1Xy}" Binding="{Binding Pillar1XY}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Pillar2Xy}" Binding="{Binding Pillar2XY}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_ZPosition}" Binding="{Binding ZPosition}" Width="100"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Parallelism}" Binding="{Binding Parallelism}" Width="80"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_Engagement}" Binding="{Binding Engagement}" Width="80"/>
                                <DataGridTextColumn Header="{lang:Lang DetailedData_Column_PeakForceZ}" Binding="{Binding ZPeakForce}" Width="100"/>
                                <DataGridTextColumn Header="Radial Force X(N)" Binding="{Binding RadialForceX}" Width="100"/>
                                <DataGridTextColumn Header="Radial Force Y(N)" Binding="{Binding RadialForceY}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 操作按钮 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                            <Button Content="{lang:Lang ExecuteIPQCInspection}" Command="{Binding RunInspectionCommand}" Margin="4"
                                    Style="{StaticResource MaterialDesignRaisedButton}" Background="#28a745" Foreground="White"/>
                            <Button Content="{lang:Lang ExportCSV}" Command="{Binding ExportCsvCommand}" Margin="4"/>
                            <Button Content="{lang:Lang Close}" Command="{Binding CloseCommand}" Margin="4"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>