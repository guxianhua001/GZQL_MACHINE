<UserControl x:Class="Module.Views.AddEditStepDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="500">
    <UserControl.Resources>

    </UserControl.Resources>
    <StackPanel Margin="16">
        <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,16">添加/编辑步骤</TextBlock>

        <!-- Step Type 下拉 -->
        <ComboBox materialDesign:HintAssist.Hint="Step Type"
                  ItemsSource="{Binding StepTypeValues}"
                  SelectedItem="{Binding Step.Step}"
                  Margin="0,8" />

        <!-- Comp Feature 下拉 -->
        <ComboBox materialDesign:HintAssist.Hint="Comp Feature"
                  ItemsSource="{Binding ComponentFeatures}"
                  SelectedItem="{Binding Step.CompFeature}"
                  Margin="0,8" />

        <!-- Site Feature 下拉 -->
        <ComboBox materialDesign:HintAssist.Hint="Site Feature"
                  ItemsSource="{Binding SiteFeatures}"
                  SelectedItem="{Binding Step.SiteFeature}"
                  Margin="0,8" />

        <!-- Camera 下拉 -->
        <ComboBox materialDesign:HintAssist.Hint="Camera"
                  ItemsSource="{Binding CameraOptions}"
                  SelectedItem="{Binding Step.Camera}"
                  Margin="0,8" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    Margin="8,0" />
            <Button Content="确定"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding OkCommand}" />
        </StackPanel>
    </StackPanel>
</UserControl>