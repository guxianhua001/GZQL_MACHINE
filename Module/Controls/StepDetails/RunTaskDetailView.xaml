<UserControl x:Class="Module.Views.RunTaskDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             Width="420">
    <materialDesign:Card UniformCornerRadius="8"
                         Padding="16">
        <materialDesign:Card.Resources>
            <ResourceDictionary Source="/ModuleCore;component/Themes/DialogTheme.xaml" />
        </materialDesign:Card.Resources>
        <StackPanel>

            <!-- 目标被动任务选择 -->
            <ComboBox ItemsSource="{Binding PassiveTaskNames}"
                      SelectedItem="{Binding SelectedTargetTaskName}"
                      materialDesign:HintAssist.Hint="{lang:Lang PSE_SelectTargetTask}"
                      Margin="0,0,0,8" />

            <!-- 提示说明 -->
            <TextBlock Text="{lang:Lang PSE_RunTaskHint}"
                       TextWrapping="Wrap"
                       Opacity="0.6"
                       FontSize="11"
                       Margin="0,0,0,16" />

            <!-- 底部按钮：保存 + 关闭 -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave"
                                                Width="18"
                                                Height="18"
                                                VerticalAlignment="Center" />
                        <TextBlock Text="{lang:Lang PSE_Save}"
                                   Margin="4,0,0,0"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Close"
                                                Width="18"
                                                Height="18"
                                                VerticalAlignment="Center" />
                        <TextBlock Text="{lang:Lang PSE_Close}"
                                   Margin="4,0,0,0"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
