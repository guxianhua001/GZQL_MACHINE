<UserControl x:Class="Module.Views.WaitDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             MaxHeight="300">
    <Border Padding="16"
            Background="{DynamicResource MaterialDesignCardBackground}"
            CornerRadius="4">
        <StackPanel>
            <!-- 标题 + 关闭按钮 -->
            <DockPanel Margin="0,0,0,12">
                <Button DockPanel.Dock="Right"
                        Content="✖"
                        Width="30"
                        Height="30"
                        Command="{Binding CloseCommand}"
                        ToolTip="{lang:Lang Close}"
                        Style="{StaticResource MaterialDesignIconButton}" />
                <TextBlock FontWeight="Bold"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Text="{Binding StepDescription, StringFormat=WAIT {0}}" />
            </DockPanel>

            <!-- 延时等待 -->
            <GroupBox Header="{lang:Lang WaitDetail_DelayWait}" Margin="0,0,0,12">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="{lang:Lang WaitDetail_DelayDuration}" VerticalAlignment="Center"/>
                    <materialDesign:NumericUpDown Grid.Row="0" Grid.Column="1"
                                                  Value="{Binding DelayValue}"
                                                  Minimum="0"
                                                  Maximum="999999"
                                                  Width="200"
                                                  HorizontalAlignment="Left" />

                    <TextBlock Grid.Row="1" Grid.Column="0"
                               Text="{lang:Lang WaitDetail_UnitHint}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="1"
                              ItemsSource="{Binding TimeUnitOptions}"
                              SelectedItem="{Binding SelectedTimeUnit}"
                              Width="100"
                              HorizontalAlignment="Left" />

                    <TextBlock Grid.Row="2" Grid.Column="0"
                               Text="{lang:Lang WaitDetail_DelayDescription}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1"
                             Text="{Binding DelayDescription}"
                             materialDesign:HintAssist.Hint="{lang:Lang WaitDetail_DescriptionHint}"
                             Width="300"
                             HorizontalAlignment="Left" />
                </Grid>
            </GroupBox>

            <!-- 确认继续 -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="{lang:Lang WaitDetail_ConfirmContinue}"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>