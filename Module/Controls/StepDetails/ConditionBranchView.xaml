<UserControl x:Class="Module.Views.ConditionBranchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Module.Converters"
             xmlns:lang="clr-namespace:Core.Markup;assembly=Core"
             MinWidth="900"
             MaxHeight="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ModuleCore;component/Themes/DialogTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{DynamicResource DialogCardBrush}"
            CornerRadius="6">

        <Grid>
            <!-- ═══ 内容区域 ═══ -->
            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- 1. 启用条件分支 -->
                    <CheckBox Content="{lang:Lang BranchDetail_EnableBranch}"
                              IsChecked="{Binding IsEnabled}"
                              Margin="16,12,16,8" />

                    <!-- 2. 输出参数 -->
                    <GroupBox Margin="16,0,16,12">
                        <GroupBox.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Export" Width="16" Height="16"
                                                         VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="{lang:Lang BranchDetail_OutputParams}" VerticalAlignment="Center" />
                            </StackPanel>
                        </GroupBox.Header>
                        <StackPanel>
                            <DataGrid ItemsSource="{Binding OutputParameters}"
                                      SelectedItem="{Binding SelectedOutputParameter}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserResizeColumns="True"
                                      materialDesign:DataGridAssist.CellPadding="4">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_ParamName}"
                                                            Width="Auto" MinWidth="200">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding DataContext.PreviousStepOutputs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          SelectedValue="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                                          SelectedValuePath="Name"
                                                          DisplayMemberPath="Name"
                                                          SelectionChanged="OnParamNameSelected"
                                                          BorderThickness="0"
                                                          Background="Transparent"
                                                          materialDesign:HintAssist.Hint="{lang:Lang BranchDetail_SelectPrevOutput}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_OutputType}"
                                                            Width="90" MinWidth="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding OutputType}" VerticalAlignment="Center"
                                                           Foreground="{DynamicResource DialogTextSecondaryBrush}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_TargetGlobalVar}"
                                                            Width="*" MinWidth="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <DockPanel>
                                                    <materialDesign:PackIcon Kind="LinkVariant"
                                                                             Width="16" Height="16"
                                                                             VerticalAlignment="Center"
                                                                             Margin="0,0,4,0"
                                                                             DockPanel.Dock="Left"
                                                                             Opacity="0.5" />
                                                    <ComboBox ItemsSource="{Binding FilteredGlobalVariableNames}"
                                                              SelectedItem="{Binding TargetGlobalVariable, UpdateSourceTrigger=PropertyChanged}"
                                                              IsEditable="True"
                                                              BorderThickness="0"
                                                              Background="Transparent" />
                                                </DockPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Left"
                                        Margin="0,8,0,0">
                                <Button Content="{lang:Lang BranchDetail_AddParam}"
                                        Margin="0,0,4,0"
                                        Command="{Binding AddOutputCommand}" />
                                <Button Content="{lang:Lang Delete}"
                                        Margin="0,0,4,0"
                                        Command="{Binding RemoveOutputCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- 3. 条件规则 -->
                    <GroupBox Margin="16,0,16,12">
                        <GroupBox.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="SourceBranch" Width="16" Height="16"
                                                         VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="{lang:Lang BranchDetail_ConditionRules}" VerticalAlignment="Center" />
                            </StackPanel>
                        </GroupBox.Header>
                        <StackPanel>
                            <DataGrid x:Name="ConditionsGrid"
                                      ItemsSource="{Binding Conditions}"
                                      SelectedItem="{Binding SelectedCondition}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserResizeColumns="True"
                                      materialDesign:DataGridAssist.CellPadding="4">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_ConditionExpr}"
                                                            Width="300" MinWidth="200">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox Grid.Column="0"
                                                             Text="{Binding ConditionExpression, UpdateSourceTrigger=PropertyChanged}"
                                                             IsReadOnly="False"
                                                             VerticalAlignment="Center"
                                                             LostFocus="OnExprTextBox_LostFocus"
                                                             AcceptsReturn="False"
                                                             materialDesign:HintAssist.Hint="@Output:xxx > 10 或 @GV:变量名 == 0"
                                                             BorderThickness="0"
                                                             Background="Transparent" />
                                                    <ComboBox Grid.Column="1"
                                                              ItemsSource="{Binding DataContext.PreviousStepOutputNames, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedIndex="-1"
                                                              SelectionChanged="OnVariableSelected"
                                                              ToolTip="{lang:Lang BranchDetail_InsertVar}"
                                                              Width="28" Height="24"
                                                              BorderThickness="0"
                                                              Background="Transparent"
                                                              Padding="0">
                                                        <ComboBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding}" FontSize="10" />
                                                            </DataTemplate>
                                                        </ComboBox.ItemTemplate>
                                                    </ComboBox>
                                                </Grid>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_JumpTo}"
                                                            Width="100" MinWidth="70">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding DataContext.AvailableStepSeqs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          SelectedValue="{Binding TargetStepSeq, UpdateSourceTrigger=PropertyChanged}"
                                                          BorderThickness="0"
                                                          Background="Transparent"
                                                          Padding="2,0" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="{lang:Lang BranchDetail_Column_Description}"
                                                        Binding="{Binding Description}"
                                                        Width="150" MinWidth="100" />

                                    <DataGridTemplateColumn Header="{lang:Lang BranchDetail_Column_Actions}"
                                                            Width="Auto" MinWidth="60">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            ToolTip="{lang:Lang BranchDetail_MoveUp}"
                                                            Style="{StaticResource MaterialDesignIconButton}"
                                                            Width="24" Height="24"
                                                            Margin="0,0,2,0">
                                                        <materialDesign:PackIcon Kind="ArrowUp" Width="14" Height="14" />
                                                    </Button>
                                                    <Button Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            ToolTip="{lang:Lang BranchDetail_MoveDown}"
                                                            Style="{StaticResource MaterialDesignIconButton}"
                                                            Width="24" Height="24">
                                                        <materialDesign:PackIcon Kind="ArrowDown" Width="14" Height="14" />
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Left"
                                        Margin="0,8,0,0">
                                <Button Content="{lang:Lang BranchDetail_AddCondition}"
                                        Margin="0,0,4,0"
                                        Command="{Binding AddConditionCommand}" />
                                <Button Content="{lang:Lang Delete}"
                                        Margin="0,0,4,0"
                                        Command="{Binding RemoveConditionCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- 4. 默认动作 -->
                    <GroupBox Margin="16,0,16,12">
                        <GroupBox.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" Width="16" Height="16"
                                                         VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="{lang:Lang BranchDetail_DefaultAction}" VerticalAlignment="Center" />
                            </StackPanel>
                        </GroupBox.Header>
                        <StackPanel Margin="8">
                            <DockPanel Margin="0,0,0,8">
                                <TextBlock Text="{lang:Lang BranchDetail_Default}" VerticalAlignment="Center"
                                           DockPanel.Dock="Left" Margin="0,0,8,0"/>
                                <ComboBox x:Name="ActionCombo"
                                          ItemsSource="{Binding DefaultActionTypes}"
                                          SelectedItem="{Binding DefaultActionText}"
                                          Width="120"
                                          HorizontalAlignment="Left">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding}" Value="Continue">
                                                                <Setter Property="Text" Value="{lang:Lang Continue}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding}" Value="Stop">
                                                                <Setter Property="Text" Value="{lang:Lang Stop}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding}" Value="SkipTo">
                                                                <Setter Property="Text" Value="{lang:Lang BranchDetail_JumpToStep}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DockPanel>
                            <DockPanel>
                                <TextBlock Text="{lang:Lang BranchDetail_JumpToStep}" VerticalAlignment="Center"
                                           DockPanel.Dock="Left" Margin="0,0,8,0"
                                           Visibility="{Binding IsSkipTo, Converter={StaticResource BoolToVis}}"/>
                                <ComboBox ItemsSource="{Binding AvailableStepSeqs}"
                                          SelectedValue="{Binding DefaultTargetStepSeq}"
                                          Width="80"
                                          IsEnabled="{Binding IsSkipTo}"
                                          Visibility="{Binding IsSkipTo, Converter={StaticResource BoolToVis}}" />
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- 5. 底部按钮 -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,8,0,16">
                        <Button Content="{lang:Lang Cancel}"
                                Command="{Binding CancelCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="16,0,8,0" />
                        <Button Content="{lang:Lang BranchDetail_Ok}"
                                Click="OnOkClick"
                                Style="{StaticResource MaterialDesignRaisedButton}" 
                                Margin="16,0,8,0"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
