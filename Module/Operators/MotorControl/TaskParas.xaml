<UserControl x:Class="Framework.Views.TaskParas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Framework.Views"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinViolet.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="Deeppurple"
                                             SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <LinearGradientBrush x:Key="WindowBackgroundBrush"
                                 StartPoint="0,0"
                                 EndPoint="1,1">
                <GradientStop Color="White"
                              Offset="0.0" />
                <GradientStop Color="White"
                              Offset="1.0" />
            </LinearGradientBrush>
            <Thickness x:Key="Marg"
                       Left="3"
                       Right="3"
                       Top="2"
                       Bottom="2" />
            <Style x:Key="ProtectedPropertyGrid"
                   TargetType="hc:PropertyGrid"
                   BasedOn="{StaticResource {x:Type hc:PropertyGrid}}">
                <Setter Property="IsEnabled"
                        Value="{Binding CanEditParams}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding CanEditParams}"
                                 Value="False">
                        <Setter Property="Opacity"
                                Value="0.7" />
                        <Setter Property="ToolTip"
                                Value="需要管理员权限且设备停止" />
                        <Setter Property="Background"
                                Value="{DynamicResource MaterialDesignDisabledBackground}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- 第一行高度自动调整 -->
            <RowDefinition Height="*" />
            <!-- 第二行占据剩余空间 -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- 一列占据全部宽度 -->
        </Grid.ColumnDefinitions>

        <!-- 第一行：按钮 -->
        <StackPanel Grid.Row="0"
                    Grid.Column="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="5">
            <Button Content="刷新"
                    Margin="5"
                    Width="100" />
            <Button Content="保存"
                    Margin="5"
                    Width="100"
                    Command="{Binding Save}" />
        </StackPanel>

        <!-- 第二行：PropertyGrid -->
        <!--<hc:PropertyGrid x:Name="myPropertyGrid"
                         Grid.Row="1"
                         Grid.Column="0"
                         Height="Auto"
                         Width="Auto"
                         MaxWidth="500"
                         MinTitleWidth="220"
                         Background="{DynamicResource MaterialDesignPaper}"
                         Foreground="{DynamicResource MaterialDesignBody}"
                         SelectedObject="{Binding TaskParas}">
        </hc:PropertyGrid>-->
        <hc:PropertyGrid x:Name="myPropertyGrid"
                         Style="{StaticResource ProtectedPropertyGrid}"
                         Grid.Row="1"
                         Grid.Column="0"
                         Height="Auto"
                         Width="Auto"
                         MaxWidth="500"
                         MinTitleWidth="220"
                         Background="{DynamicResource MaterialDesignPaper}"
                         Foreground="{DynamicResource MaterialDesignBody}"
                         SelectedObject="{Binding TaskParas}">
        </hc:PropertyGrid>
    </Grid>
</UserControl>
